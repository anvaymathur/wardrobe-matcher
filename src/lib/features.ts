// Feature switches. Shuffle (and its /builder companion) is parked while the AI
// stylist takes over outfit ideas — the code stays so it can be turned back on.
export const FEATURES = {
  shuffle: false,
} as const;
